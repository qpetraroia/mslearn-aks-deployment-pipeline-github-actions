apiVersion: v1
kind: Service
metadata:
    name: "kubecondemo-850f"
    labels:
        app: "kubecondemo-850f"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubecondemo-850f"